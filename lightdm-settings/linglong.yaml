version: "1"
package:
  id: lightdm-settings.linyaps
  name: lightdm-settings
  version: 2.0.1.1
  kind: app
  description: |-
    LightDM Settings Configuration Tool
    This package assists in settings configuration for the LightDM
    display manager.

    This tool currently lets users configure slick-greeter.
command:
- /opt/apps/lightdm-settings.linyaps/files/entrypoint.sh
base: org.deepin.base/23.1.0
build: build-aux/setup.sh
